import { Types } from "mongoose";

// TODO: implement this service
export const deleteUserWithCleanup = async (user: any) => {
    try {
    } catch (error) {
        console.error("DELETE USER WITH CLEANUP ERROR : ", error);
        throw error;
    }
};
